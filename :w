#ifndef _LINE
#define _LINE
#include "Point.h"
#include <cmath>
#include <iostream>
class Line {
public:
  Line(SDL_Renderer *renderer, Point *p1, Point *p2);
  static void Line::renderNaiveLine(SDL_Renderer *renderer, Point p1, Point p2);
  static void renderDDALine(SDL_Renderer *renderer, Point p1, Point p2);
};
#endif
